/**
 * SPHYNX desert palette — sun-baked sand gold on a night-dune canvas.
 * These mirror the tokens in app/globals.css. This module exists because SVG
 * fill/stroke and <canvas> cannot read CSS custom properties reliably.
 * (Export names are kept from the original theme so downstream code is untouched:
 *  LIME = the sand accent, INK = the cream foreground.)
 */
export const LIME = "#E8B85A"; // sand gold accent
export const INK = "#F3E9D2"; // cream foreground marks on dark dune
export const RED = "#E2664A"; // terracotta — eyes / danger / accents
export const GREEN = "#E8B85A"; // up candles
export const GREEN_DEEP = "#A67C33"; // deep sand
export const MINT = "#F3D48A"; // pale sand
export const WARN = "#F5A742"; // sunset amber
export const WHITE = "#F3E9D2";
